__pycache__/
*.py[cod]
.venv/
.env
*.parquet
# generated dataset export — attaches to the versioned DOI'd release, not committed
release/
.DS_Store
dist/
build/
*.egg-info/
# local Claude Code guidance — kept locally, untracked
CLAUDE.md
# drawn evaluation sample (contains public-record names) — local working artifact
eval_out/
